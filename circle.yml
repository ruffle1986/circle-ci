machine:
  node:
    version: 4.2.1
  environment:
    repo: https://$GITHUB_ACCESS_TOKEN@github.com/$CIRCLE_PROJECT_USERNAME/$CIRCLE_PROJECT_REPONAME.git

deployment:
  production:
    branch: master
    commands:
      - echo $repo
      - pwd
